from setuptools import setup, find_packages

setup(
    name='neural-network-from-scratch',
    version='0.2.0',
    description='NumPy neural network from scratch with hardware-aware profiling',
    packages=find_packages(),
    install_requires=['numpy>=1.20'],
)
