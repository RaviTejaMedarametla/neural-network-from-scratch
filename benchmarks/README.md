# Benchmarks Directory Layout

Expected generated files:
- `benchmark_results.csv`: baseline benchmarking sweep output.
- `statistical/`: repeated-run summaries, confidence intervals, and trade-off plots.
- `comparison/`: scratch vs framework comparison artifacts.

Benchmark outputs are reproducible products and should be regenerated from scripts rather than edited manually.
